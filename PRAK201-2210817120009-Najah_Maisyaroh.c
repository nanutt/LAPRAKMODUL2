#include <stdio.h>
int main(){
    char Nama[20], nim[20], kelas[15], Ttl[50], Alamat[50], Hobby[20], nohp[20];
    printf("Nama                   :");
    gets(Nama);
    printf("NIM                    :");
    gets(nim);
    printf("Kelas Paralel          :");
    gets(kelas);
    printf("Tempat/ Tanggal Lahir  :");
    gets(Ttl);
    printf("Alamat                 :");
    gets(Alamat);
    printf("Hobby                  :");
    gets(Hobby);
    printf("No.HP                  :");
    gets(nohp);
    return 0;
}